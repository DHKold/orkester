apiVersion: orkester.io/v1
kind: Namespace
name: default
version: "1.0.0"
metadata:
  description: "Default namespace for general-purpose workflows"
spec: {}
---
apiVersion: orkester.io/v1
kind: Namespace
name: data-platform
version: "1.0.0"
metadata:
  description: "Data platform pipelines — ingestion, transformation, and delivery"
  labels:
    team: data-engineering
    env: production
spec:
  max_concurrent_workflows: 10
---
apiVersion: orkester.io/v1
kind: Namespace
name: sandbox
version: "1.0.0"
metadata:
  description: "Sandbox namespace for development and testing"
  labels:
    team: data-engineering
    env: dev
spec:
  max_concurrent_workflows: 3
